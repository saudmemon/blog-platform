---
description: Start the blogging platform (client and server)
---
1. Ensure MongoDB is running on your machine or you have a valid MONGO_URI in `server/.env`.
// turbo
2. Run the development environment:
   ```bash
   npm run dev
   ```
3. Open your browser and navigate to `http://localhost:5173`.
